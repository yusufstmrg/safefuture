(function(){'use strict';const reduce=window.matchMedia&&window.matchMedia('(prefers-reduced-motion: reduce)').matches;function init(){const navLinks=[...document.querySelectorAll('#navbar a[href^="#"]')];const sections=navLinks.map(a=>({a,id:a.getAttribute('href').slice(1)})).filter(x=>document.getElementById(x.id));if(!reduce){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){sections.forEach(x=>x.a.classList.remove('nav-active'));const hit=sections.find(x=>x.id===e.target.id);if(hit)hit.a.classList.add('nav-active')}}),{rootMargin:'-25% 0px -60% 0px',threshold:0});sections.forEach(x=>io.observe(document.getElementById(x.id)))} }if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init()})();