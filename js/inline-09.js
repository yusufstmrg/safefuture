(function(){'use strict';const reduce=window.matchMedia&&window.matchMedia('(prefers-reduced-motion: reduce)').matches;function init(){const targets=[...document.querySelectorAll('section > div, .premium-card, .glass-card, .sf-product-card, .sf-asset-card, .timeline-item-vert, #priorityList > div')].filter(el=>!el.closest('.hidden'));targets.forEach((el,i)=>{if(!el.classList.contains('sf-reveal')){el.classList.add('sf-reveal');const d=i%5;if(d)el.classList.add('sf-delay-'+d)}});if(reduce){targets.forEach(el=>el.classList.add('sf-visible'));return}const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('sf-visible');io.unobserve(e.target)}}),{rootMargin:'0px 0px -8% 0px',threshold:.08});targets.forEach(el=>io.observe(el))}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init()})();